# Question:
# 2 is written as II in Roman numeral, just two one's added together. 12 is written as XII, which is simply X + II. The number 27 is written as XXVII, which is XX + V + II.
# Roman number is weitten largest to smallest from leftt to right. However, the numberal for four is not IIII. instead, the number four is written as IV. Because the one is before five we subtract it making four. The same principle applies to the number nine, which is written as IX. There are six instances where subtraction is used:
# Given a roman numeral, convert it to an integer. 
# - I can be placed befire V (5) and X (10) to make 4 and 9
# - X can be placed before L (50) and C (100) to make 40 and 90.
# - C can be placed before D (500) and M (1000) to make 400 and 900.
# Given a roman numeral, convert it to an integer.


# The function is expected to return an INTEGER.
# The function accepts STRING s as parameter.
#

def roman_to_int(s):
    # Write your code here
    roman_mapped = {'I': 1, 'V': 5, 'X': 10, 'L': 50, 'C': 100, 'D': 500, 'M':1000}
    total = 0
    for i in range(len(s)):
        current = roman_mapped[s[i]]
        if i < len(s) - 1 and current < roman_mapped[s[i+1]]:
            total -= current
        else:
            total += current
    return total

# if __name__ == '__main__':    
#     outfile = open(os.environ['OUTPUT_PATH'], 'w')
#     input_data = sys.stdin.read().strip().split('\n')
    
#     for line in input_data:
#         s = line.strip().strip('"')  
#         result = roman_to_int(s)
#         outfile.write(str(result) + '\n')
#     outfile.close()
str1 ="III"
str2 = "LVIII"
str3 = "MCMXCIV"    
print(roman_to_int(str1))
print(roman_to_int(str2))
print(roman_to_int(str3))